module IdeasHelper

	# def grid ({:objs => @products, :columns => [] :sortable => []})
		
	# 	columns.each { |c| content_tag(:tr, content_tag(:th, sortable.include?(c) ? content_tag(:link, c) : c))}
		
	# 		objs.each { |e|

	# 			content_tag(:tr, content_tag(:td, e.name))

	# 		}
	# end	

	
end
